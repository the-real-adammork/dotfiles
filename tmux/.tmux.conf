
set -g default-terminal "tmux-256color"
set -as terminal-overrides ",xterm*:Tc"
set -as terminal-overrides ",*:Smulx=\E[4::%p1%dm"
set -as terminal-overrides ",*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m"

set-option -g mouse on

# Copy mode
set -g mode-keys vi
unbind [
bind Escape copy-mode
unbind p
bind p paste-buffer

unbind C-b
set -g prefix `
bind-key ` send-prefix

set -sg escape-time 1 # quicker responses
bind C-a send-prefix # Pass on ctrl-a for other apps
set -g base-index 1 # Numbering of windows
set -g pane-base-index 1 # Numbering of Panes
bind c new-window      -c "#{pane_current_path}"
bind \\ split-window -h -c "#{pane_current_path}" # Split panes horizontal
bind - split-window -v -c "#{pane_current_path}" # Split panes vertically

# set history buffer size to be massive
set-option -g history-limit 10000

# Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-online-status'
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'catppuccin/tmux'
set -g @plugin 'hiroppy/tmux-agent-sidebar'

# Configure Catppuccin
set -g @catppuccin_flavor "latte"
set -g @catppuccin_status_background "none"
set -g @catppuccin_window_status_style "none"
set -g @catppuccin_pane_status_enabled "off"
set -g @catppuccin_pane_border_status "off"

# tmux-agent-sidebar Catppuccin Latte colors
set -g @sidebar_color_accent 27
set -g @sidebar_color_border 249
set -g @sidebar_color_all 38
set -g @sidebar_color_running 70
set -g @sidebar_color_waiting 172
set -g @sidebar_color_idle 37
set -g @sidebar_color_error 161
set -g @sidebar_color_filter_inactive 248

set -g @sidebar_color_agent_claude 202
set -g @sidebar_color_agent_codex 99
set -g @sidebar_color_agent_opencode 38

set -g @sidebar_color_text_active 240
set -g @sidebar_color_text_muted 243
set -g @sidebar_color_text_inactive 102
set -g @sidebar_color_session 27
set -g @sidebar_color_port 246
set -g @sidebar_color_wait_reason 172
set -g @sidebar_color_selection 252

set -g @sidebar_color_branch 30
set -g @sidebar_color_task_progress 172
set -g @sidebar_color_subagent 30
set -g @sidebar_color_commit_hash 172
set -g @sidebar_color_diff_added 70
set -g @sidebar_color_diff_deleted 167
set -g @sidebar_color_file_change 172
set -g @sidebar_color_pr_link 38
set -g @sidebar_color_section_title 30
set -g @sidebar_color_activity_timestamp 30
set -g @sidebar_color_response_arrow 38


# Configure Online
set -g @online_icon "ok"
set -g @offline_icon "nok"

# status left look and feel
set -g status-left-length 100
set -g status-left ""
set -ga status-left "#{?client_prefix,#{#[bg=#{@thm_red},fg=#{@thm_bg},bold]  #S },#{#[bg=#{@thm_bg},fg=#{@thm_green}]  #S }}"
set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_overlay_0},none]│"
set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_maroon}]  #{pane_current_command} "
set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_overlay_0},none]│"
set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_blue}]  #{=/-32/...:#{s|$USER|~|:#{b:pane_current_path}}} "
set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_overlay_0},none]#{?window_zoomed_flag,│,}"
set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_yellow}]#{?window_zoomed_flag,  zoom ,}"

# status right look and feel
set -g status-right-length 100
set -g status-right ""
set -ga status-right "#{?battery_percentage,#{?#{e|>=:10,#{battery_percentage}},#{#[bg=#{@thm_red},fg=#{@thm_bg}]},#{#[bg=#{@thm_bg},fg=#{@thm_pink}]}} #{battery_icon} #{battery_percentage} #[bg=#{@thm_bg}#,fg=#{@thm_overlay_0}#, none]│,}"
set -ga status-right "#[bg=#{@thm_bg}]#{?#{==:#{online_status},ok},#[fg=#{@thm_mauve}] 󰖩 on ,#[fg=#{@thm_red},bold]#[reverse] 󰖪 off }"
set -ga status-right "#[bg=#{@thm_bg},fg=#{@thm_overlay_0}, none]│"
set -ga status-right "#[bg=#{@thm_bg},fg=#{@thm_blue}] 󰭦 %Y-%m-%d 󰅐 %H:%M "

# VIM movement commands for scroll buffers
setw -g mode-keys vi
set -g status-keys vi

# Setup 'v' to begin selection, just like Vim
bind-key -T copy-mode-vi 'v' send -X begin-selection

 # Setup 'y' to yank (copy), just like Vim
bind-key -T copy-mode-vi 'y' send -X copy-pipe-and-cancel "pbcopy"
bind-key -T copy-mode-vi 'V' send -X select-line
bind-key -T copy-mode-vi 'r' send -X rectangle-toggle

# VI-Like movement for panes
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# choose window/session
bind-key "'" choose-window
bind-key '"' choose-session
bind-key S command-prompt -p "New session name:" "new-session -s '%%' -c '#{pane_current_path}'"

bind R source-file ~/.tmux.conf \; display-message "Config reloading..."

# bootstrap tpm
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

# Configure Tmux
set -g status-position top
set -g status-style "bg=#{@thm_bg}"
set -g status-justify "absolute-centre"

# pane border look and feel
setw -g pane-border-status top
setw -g pane-border-format "#{?pane_active,#[bg=#{@thm_blue},fg=#{@thm_bg},bold] #I.#P #{pane_current_command} #[default],}"
setw -g pane-active-border-style "bg=#{@thm_bg},fg=#{@thm_blue},bold"
setw -g pane-border-style "bg=#{@thm_crust},fg=#{@thm_surface_0}"
setw -g pane-border-lines single

# Dim inactive panes
set -g window-style "bg=#{@thm_crust}"
set -g window-active-style "bg=#{@thm_bg}"

# window look and feel
set -wg automatic-rename on
set -g automatic-rename-format "Window"

set -g window-status-format " #I#{?#{!=:#{window_name},Window},: #W,} "
set -g window-status-style "bg=#{@thm_bg},fg=#{@thm_rosewater}"
set -g window-status-last-style "bg=#{@thm_bg},fg=#{@thm_peach}"
set -g window-status-activity-style "bg=#{@thm_red},fg=#{@thm_bg}"
set -g window-status-bell-style "bg=#{@thm_red},fg=#{@thm_bg},bold"
set -gF window-status-separator "#[bg=#{@thm_bg},fg=#{@thm_overlay_0}]│"

set -g window-status-current-format " #I#{?#{!=:#{window_name},Window},: #W,} "
set -g window-status-current-style "bg=#{@thm_peach},fg=#{@thm_bg},bold"

# Switch back to last session/window
bind Tab switch-client -l

# Disable layout switching which is super annoying
unbind space
