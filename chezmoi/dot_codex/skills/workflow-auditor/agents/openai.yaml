interface:
  display_name: "Workflow Auditor"
  short_description: "Audit agent workflow logs and improve process"
  default_prompt: "Use the workflow-auditor skill to analyze supervisor, orchestrator, and worker logs for workflow improvements."
