interface:
  display_name: "Secrets"
  short_description: "Prevent unsafe secret disclosure or commits"
  default_prompt: "Use $secrets to protect suspected secret material from unsafe inspection, disclosure, staging, or commits."
