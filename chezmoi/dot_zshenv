cargo_paths="${CARGO_HOME:-$HOME/.cargo}/bin"
if command -v brew &>/dev/null && rustup_prefix="$(brew --prefix rustup 2>/dev/null)"; then
    cargo_paths="$cargo_paths:$rustup_prefix/bin"
fi
export PATH="$cargo_paths:$PATH"
unset cargo_paths rustup_prefix
export EDITOR="nvim"
unset BAT_CONFIG_PATH
