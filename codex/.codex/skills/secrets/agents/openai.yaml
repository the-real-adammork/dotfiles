interface:
  display_name: "Secrets"
  short_description: "Handle generated and protected secrets"
  default_prompt: "Use  to classify, generate, store, encrypt, ignore, or verify secrets and credentials safely."
