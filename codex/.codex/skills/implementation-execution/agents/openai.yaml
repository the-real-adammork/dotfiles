interface:
  display_name: "Implementation Execution"
  short_description: "Run phase plans with YAML state"
  default_prompt: "Use $implementation-execution to run approved implementation phase plans with the top-level session as supervisor, spawned phase orchestrators when dispatch is available, supervisor-spawned bounded workers, TDD gates, YAML state, and phase acceptance evidence."
